using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GA : MonoBehaviour
{ 
    
    // Start is called before the first frame update
    void Start()
    {
        // Initial random population of rooms: (Place randomly, but always make sure they are not overlapping - or at least try)



        // Fitness function: assess each member of population by their fitness function. 

        // Selection: The fittest individuals are selected on fitness score (how many)
        // The fitness function is the probability of being chosen

        // Crossover: New member from existing members

        // Mutation



    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void InitialPopulation()
    {
        
    }
}
